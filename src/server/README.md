# src/server/ — backend service layer

Policy (docs/REPO_STANDARD.md §2): api, workers, and storage adapters live here.
This repo ships a headless kernel, so the folder is reserved. First service
layer moves this repo's class to `fullstack` in seeds/seeds.yaml + package.json.
